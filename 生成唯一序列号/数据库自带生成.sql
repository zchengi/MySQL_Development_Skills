CREATE TABLE t (
  id INT AUTO_INCREMENT NOT NULL,
  PRIMARY KEY (id)
);

INSERT INTO t VALUES (), ();
SELECT *
FROM t;

BEGIN;
INSERT INTO t VALUES ();
SELECT *
FROM t;

ROLLBACK;
INSERT INTO t VALUES ();
SELECT *
FROM t;

